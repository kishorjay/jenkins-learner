pipeline {
    agent any

    stages {
        stage('Started Pipe') {
            steps {
                echo 'intiated'
            }
        }
            
        stage('Multi branch pipe') {
			steps {
				echo 'my multi-branch pipe'
			}
        }
        
    }
}
