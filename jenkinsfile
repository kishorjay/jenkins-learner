pipeline {
    agent any

    stages {
        stage('Started Pipe') {
            steps {
                echo 'intiated'
            }
        }
            
        stage('Build') {
			steps {
				echo 'performing build stage'
			}
        }
		stage('Parellels') {
        parallel {
			stage('Test') {
				steps {
					echo 'performing test stage'
			}
			}
			stage('Deploy') {
				steps {
					echo 'performing deploy stage'
			}
			}
	} 
   }
  }
}
