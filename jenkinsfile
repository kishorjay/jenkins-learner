pipeline {
    agent any

    stages {
        stage('Started Pipe') {
            steps {
                echo 'intiated'
            }
        }
            
        stage('Build') {
			environment {
			        def kj = ""
			        kj = bat(script : "echo hello", returnStdout: true)
				
			}
			steps {
				echo 'performing build stage'
				echo kj

			}
        }
		stage('Parellels') {
        parallel {
			stage('Test') {
				steps {
					echo 'performing test stage'
			}
			}
			stage('Deploy') {
				steps {
					echo 'performing deploy stage'
			}
			}
	} 
   }
  }
}
